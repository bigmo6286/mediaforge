#!/usr/bin/env bash
# Double-click to start MediaForge on macOS (opens in Terminal).
# First time: right-click > Open (to bypass Gatekeeper), or run `chmod +x MediaForge.command`.
cd "$(dirname "$0")"
exec ./run.sh
