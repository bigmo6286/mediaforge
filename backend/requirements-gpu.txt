# GPU model stack (diffusers etc.) for running Wan / LTX-Video locally.
#
# IMPORTANT: torch + torchvision are NOT listed here on purpose. On Windows,
# `--extra-index-url` often resolves to the CPU-only Torch wheel from PyPI, so
# the GPU never activates. The setup scripts install Torch explicitly from the
# CUDA index instead:
#
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# setup_gpu.ps1 / setup_gpu.sh do this for you. Then this file provides the rest:

diffusers>=0.31          # WanPipeline, LTXPipeline, LTXImageToVideoPipeline
transformers>=4.44
accelerate>=0.30
sentencepiece>=0.2       # text encoders (umT5 etc.)
imageio[ffmpeg]>=2.34    # export_to_video

# Talking avatar (local) is run from a SadTalker checkout, not pip — see README:
#   git clone https://github.com/OpenTalker/SadTalker && set SADTALKER_DIR
