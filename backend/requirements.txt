# Core API — required
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
httpx>=0.27
imageio-ffmpeg>=0.5.1      # bundles an ffmpeg binary (no sudo needed)

# Local image editing — optional but light
Pillow>=10.0

# NOTE: local text-to-speech (Piper) is installed SEPARATELY as a best-effort
# step by run.sh / run.ps1 (see requirements-voice.txt). It's kept out of the
# core list because its phonemizer wheel can fail to build on some Windows
# setups, and a voice add-on must never block the app from installing.

# Background removal — optional, heavier (ONNX). Uncomment to enable:
# rembg>=2.0
# onnxruntime>=1.17

# Local face swap — optional (CPU-capable). Also set INSWAPPER_MODEL to the
# inswapper_128.onnx path. Uncomment to enable FACESWAP_PROVIDER=local:
# insightface>=0.7
# onnxruntime>=1.17
# opencv-python>=4.9

# Local face restore (sharpen swapped/blurry faces) — optional. Uncomment for
# RESTORE_PROVIDER=local; weights auto-download on first run:
# gfpgan>=1.3.8
# basicsr>=1.4.2
# facexlib>=0.3
# realesrgan>=0.3

# Local Wan on a CUDA GPU — optional, very heavy. Install only on a GPU box:
# torch --index-url https://download.pytorch.org/whl/cu121
# diffusers>=0.31
# transformers>=4.44
# accelerate>=0.30
